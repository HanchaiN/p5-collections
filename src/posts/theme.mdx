---
title: Theme
slug: theme
type: WIP
---

import * as style from "@/styles/main.module.scss";

# Theme Testing

<div className={`${style.primary}`}>Primary</div>

<div className={`${style.primaryContainer}`}>PrimaryContainer</div>

<div className={`${style.secondary}`}>Secondary</div>

<div className={`${style.secondaryContainer}`}>SecondaryContainer</div>

<div className={`${style.tertiary}`}>Tertiary</div>

<div className={`${style.tertiaryContainer}`}>TertiaryContainer</div>

<div className={`${style.error}`}>Error</div>

<div className={`${style.errorContainer}`}>ErrorContainer</div>

<div className={`${style.surface}`}>Surface</div>

<div className={`${style.surfaceContainer}`}>SurfaceContainer</div>
